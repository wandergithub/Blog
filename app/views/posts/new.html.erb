<%= form_with model: post, url: "/users/#{user_id}/posts" , local: true, scope: :post do |form| %>
  <div>
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :text %>
    <%= form.text_area :text, size: "70x5" %>
  </div>

  <%= form.submit "Create post" %>
<% end %>
<% if flash[:error] %>
  <div class="notice"><%= flash[:error] %></div>
<% end %>