<div class="user-c">
  <img
    src="<%= user.photo %>"
    alt="profile default picture"
    width="90"
    height="90"
  >
  <div>
    <h1>
    <%= link_to "#{user.name}", user_path(id: user.id) %>
    </h1>
    <h3>
    Number of posts:
    <%= user.PostsCounter %>
    </h3>
    
    
  </div>
</div>
